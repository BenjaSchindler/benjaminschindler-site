# "Ask the Agent" section (app/api/agent). Without a key the demo serves
# recorded replay sessions — the site works either way.
# Set the same variable in the Vercel project settings for production.
OPENAI_API_KEY=
# Optional model override (default: gpt-5.4-mini, run with reasoning effort "none")
AGENT_MODEL=
